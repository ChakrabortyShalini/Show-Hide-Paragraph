# Show-Hide-Paragraph
This project demonstrates how to show and hide a paragraph using a button. It uses basic HTML for structure, CSS for styling, and JavaScript to toggle the paragraph’s visibility dynamically when the button is clicked.
